# 뷰니스 공동구매 사업부 모니터링 시스템 — 설계서

**버전:** v1.0 (MVP) | **작성일:** 2026-08-25 | **대상:** BEAUNESS 공동구매 사업부, 마케팅팀, 경영진

---

## 1. 목표와 범위

인스타그램·유튜브 등에서 진행되는 사내외 공동구매(이하 "공구")를 한 곳에 모아
① 카테고리별 ② 셀러(인플루언서)별로 과거·현재·미래 현황을 파악하고,
③ 건별 상세정보(제품, 채널, 예상매출)를 확인하며,
④ 오픈마켓·홈쇼핑 등 시장 수요 데이터를 함께 분석해 **다음 공구 히트 제품을 미리 예측**하는 것이 목표입니다.

최종 산출물은 **사내 누구나 링크 하나로 접속해 보는 대시보드(프로그램)** 입니다.

---

## 2. 전체 아키텍처

기존 뷰니스 사내 도구(`pricing_search`)와 동일한 구조를 채택합니다. 별도 서버·DB 구축 없이
**운영비 0원, 유지보수 최소화**로 빠르게 굴릴 수 있고, 비개발자(담당 직원)가 구글시트만 다룰 줄 알면
데이터 입력·수정이 가능하다는 장점이 있습니다.

```
┌───────────────────┐      담당자 수동 입력       ┌──────────────────────┐
│  공동구매마스터 등  │ ◄────────────────────────── │  각 사업부 담당자     │
│  구글시트 (DB 역할) │                              │  (마케팅/사업개발)    │
│                    │      API 자동 적재(파이썬 배치)│                      │
│  - 마스터 시트 7종  │ ◄────────────────────────── │  YouTube API 등       │
└─────────┬──────────┘                              └──────────────────────┘
          │ gviz JSON (공개 게시된 시트를 실시간 조회)
          ▼
┌───────────────────────────────────────────┐
│  정적 대시보드 (HTML/JS, GitHub Pages 배포) │
│  - 카테고리별 탭 / 셀러별 탭 / 상세정보 탭   │
│  - 예측 인사이트 탭                         │
│  - 1분 주기 자동 새로고침, 검색/필터/정렬    │
└───────────────────────────────────────────┘
```

- **DB 역할**: Google Sheets 1개 (다중 탭). 파일은 이번 작업 결과물인
  `공동구매_모니터링_템플릿.xlsx` 를 업로드해 시작합니다.
- **대시보드**: `dashboard/index.html` 정적 페이지. 구글시트를 "웹에 게시(File → Share → Publish to web)"
  한 뒤 시트ID·gid를 대시보드 설정값에 넣으면 별도 배포 작업 없이 바로 연동됩니다.
- **배포**: 신규 GitHub 저장소(`gongu_monitor`, 독립 구축)에 올리고 GitHub Pages를 켜면
  `https://{계정}.github.io/gongu_monitor/dashboard/` 형태의 사내 공유 링크가 생깁니다.
- **보안**: 구글시트를 "링크가 있는 모든 사용자 - 뷰어"로 제한 공유하거나, 사내망에서만 쓰는 경우
  워크스페이스 도메인 제한 공유를 권장합니다. 민가 정보(원가, 마진 등)는 별도 비공개 시트로 분리하는 것도 검토하세요.

---

## 3. 데이터 모델 (구글시트 7개 탭)

| 탭 | 성격 | 담당 | 핵심 컬럼 |
|---|---|---|---|
| **기본가정** | 입력(노란색) | 사업부 리더 | 기준연월, 예측점수 가중치(셀러/모멘텀/시장수요) |
| **카테고리마스터** | 입력 | 사업부 | 카테고리ID, 대분류, 소분류 |
| **셀러마스터** | 입력 | 마케팅 | 셀러ID, 셀러명, 플랫폼, 핸들/URL, 팔로워·구독자수, 등급(나노/마이크로/매크로/메가), 주요카테고리, 소속MCN/연락처 |
| **채널마스터** | 입력 | 마케팅 | 채널ID, 채널유형(인스타/유튜브/카페/오픈채팅/자사몰 등), 채널명, URL |
| **공동구매마스터** | 입력 (핵심) | 담당자 전원 | No, 제품명(SKU), 카테고리ID, 셀러ID, 진행채널ID, 오픈일, 마감일, 상태(자동계산), 정가, 판매가, 할인율(자동), 목표판매수량, 실판매수량, 예상매출액(자동), 실매출액(자동), 수수료율, 예상순이익(자동), 데이터출처, 등록자, 등록일, 비고 |
| **시장수요트래킹** | 입력 | 마케팅/사업개발 | No, 제품/키워드, 카테고리ID, 채널유형(오픈마켓/홈쇼핑/검색트렌드), 채널명, 지표유형, 수요지수(1~100 정규화), 수집일, 데이터출처, 비고 |
| **예측점수** | 자동계산 | — | 카테고리ID, 셀러영향력점수, 공구모멘텀점수, 시장수요점수, 종합히트예상점수, 등급(S/A/B/C) |
| **대시보드** | 자동계산 | — | KPI 요약, 카테고리별 비중, 셀러 매출 TOP10, 히트예상 TOP10, 경고 |

**공동구매마스터**가 시스템의 중심(Fact 테이블) 이고, 나머지는 마스터/집계 테이블입니다.
색상 규칙은 기존 현금·재고 템플릿과 동일합니다: 🟡 직접입력 / 🔵 수식(수정금지) / 🔴 경고 / 🟠 마스터 불일치.

### 상태 자동판정 로직
```
상태 = IF(오늘 < 오픈일, "예정", IF(오늘 <= 마감일, "진행중", "종료"))
```
이 값 하나로 "과거/현재/미래" 필터가 카테고리별·셀러별 탭 모두에서 공통으로 동작합니다.

---

## 4. 데이터 수집 전략 (자동화 가능 범위 최대화)

요청하신 대로 "가능한 자동화를 최대한 넓게" 검토했습니다. 채널별로 자동화 가능성이 크게 다릅니다.

| 데이터 소스 | 자동화 가능성 | 방법 | 비고 |
|---|---|---|---|
| **유튜브** (공구 영상 조회수·좋아요·업로드일) | ✅ 높음 | YouTube Data API v3 (무료 쿼터) | 특정 채널 구독 후 신규 영상에 "공구/공동구매/할인코드" 키워드 자동 탐지 가능 |
| **네이버 검색어 트렌드** | ✅ 높음 | 네이버 데이터랩 오픈API (무료) | 카테고리·키워드별 검색량 지수 자동 수집 |
| **네이버쇼핑 인기도/가격** | ✅ 중간 | 네이버쇼핑 검색 오픈API | 상품 검색 순위·가격 비교 조회 가능 |
| **쿠팡 베스트/순위** | ⚠️ 조건부 | 쿠팡 파트너스 API | 파트너스 가입 및 상업적 이용약관 확인 필요, 활용 범위 제한적 |
| **홈쇼핑 편성/매출** | ❌ 낮음 | 공식 오픈API 없음 | 각 사 홈페이지 자동 수집은 이용약관 검토 필요 → **1단계는 담당자 수동 체크** 권장 |
| **인스타그램** (타 인플루언서 게시물) | ❌ 낮음 | — | Meta 공식 API(Graph API)는 **본인 소유 비즈니스 계정만** 접근 가능. 타 계정 게시물 자동 수집은 Instagram 이용약관 위반 소지가 있어 권장하지 않음 |

**인스타그램 대안 3가지 (택1 또는 병행, 별도 의사결정 필요):**
1. **수동 로깅 유지 (권장, 즉시 가능)**: 담당자가 발견 즉시 구글시트에 입력. 셀러당 팔로우해두면 하루 5~10분 내 체크 가능한 수준.
2. **국내 소셜리스닝/인플루언서 분석 유료 툴 연동** (예: 블랙키위, 아이언마우스류 등): 툴 자체 약관 하에 합법적으로 수집된 데이터를 API/CSV로 받아 우리 시트에 적재. 비용 발생, 별도 계약 검토 필요.
3. **셀러 본인 계정을 비즈니스 계정 + Graph API로 연동**: 뷰니스와 공식 파트너십을 맺은 셀러가 자기 계정을 뷰니스 앱에 권한 위임하면 본인 게시물 데이터(도달, 참여율)는 합법적으로 자동 수집 가능. 장기적으로 가장 깨끗한 방법.

**1단계(MVP) 결론**: 공동구매마스터·셀러마스터는 수동 입력으로 100% 채우고,
시장수요트래킹의 "검색트렌드"·"유튜브 지표" 항목만 API 자동 적재 스크립트(2단계 과제)로 보완합니다.
자동화 스크립트는 Python(`google-api-python-client`, 네이버 오픈API REST 호출)으로 별도 배치 작업(예: 매일 새벽 GitHub Actions cron)으로 만들 수 있으며, 이는 시트 구조가 확정된 뒤 2단계로 진행을 권장합니다.

---

## 5. 히트 예상 스코어링 모델

초기(1단계)는 **설명 가능한 규칙 기반 스코어**로 시작하고, 데이터가 쌓이면(2~3단계) 회귀/분류 모델로 고도화합니다.

### 5.1 점수 구성 요소 (100점 만점, 가중치는 `기본가정` 시트에서 조정 가능)

| 요소 | 기본 가중치 | 산출 방법 |
|---|---|---|
| **셀러영향력점수** | 40% | 해당 카테고리에서 최근 진행한 셀러들의 등급(나노=1~메가=4)과 과거 실매출액 평균을 정규화 |
| **공구모멘텀점수** | 30% | 최근 3개월 해당 카테고리 공구 건수·매출 증가율 (`공동구매마스터` SUMIFS/COUNTIFS 기반) |
| **시장수요점수** | 30% | `시장수요트래킹`의 최근 검색트렌드·오픈마켓 지수 평균 (카테고리 매칭) |

```
종합히트예상점수 = 셀러영향력점수×가중치1 + 공구모멘텀점수×가중치2 + 시장수요점수×가중치3
등급 = S(≥80) / A(≥60) / B(≥40) / C(그 외)
```

### 5.2 "히트 공구가 만들어지는 구조"에 대한 가설 (분석 프레임)

데이터가 쌓이면 아래 가설들을 실제 상관관계로 검증할 수 있습니다. 대시보드 "예측 인사이트" 탭에
카테고리별 산점도(셀러영향력 vs 실매출)를 넣어 육안으로도 패턴을 확인하도록 설계했습니다.

1. **시장 선행 신호**: 오픈마켓 베스트 진입 → 4~8주 내 공구 등장 → 공구 매출 급증 패턴이 있는지
2. **셀러 등급별 전환율 차이**: 마이크로 인플루언서가 오히려 카테고리 적합도가 높으면 전환율(매출/팔로워)이 높은지
3. **가격 할인율 임계값**: 특정 할인율(예: 30% 이상) 구간에서 판매량이 비선형으로 튀는지
4. **반복 공구 효과**: 동일 셀러가 동일 제품을 재진행할 때 매출 체감/체증 여부

### 5.3 고도화 로드맵
- 3단계: `공동구매마스터` 6개월 이상 축적 후, 목표판매수량 대비 실판매수량을 타깃으로 한
  선형회귀/로지스틱 분류(파이썬 scikit-learn) 모델로 전환. 구글시트 수식 한계를 넘는 부분은
  파이썬 배치로 계산 후 결과만 `예측점수` 시트에 다시 써넣는 구조 권장.

---

## 6. 대시보드 화면 설계 (MVP, `dashboard/index.html`)

4개 탭 구조, `pricing_search`와 동일하게 검색창 + 필터 + 정렬 가능한 표 형태를 기본으로 하되
탭별로 요약 카드(KPI)를 상단에 추가합니다.

1. **카테고리별 탭**: 카테고리 선택 → 과거/현재/예정 상태 필터 → 공구 리스트 (제품, 셀러, 채널, 기간, 매출)
2. **셀러별 탭**: 셀러 선택 → 진행 이력 타임라인 형태 리스트, 누적 매출/평균 전환율 요약 카드
3. **상세정보 탭**: 리스트에서 행 클릭 → 우측 패널/모달로 제품정보·채널·예상 매출·실매출 상세 표시
4. **예측 인사이트 탭**: 히트예상 TOP10 카드, 카테고리별 종합점수 랭킹, 산점도(셀러영향력 vs 실매출)

---

## 7. 운영 프로세스 (역할 분담)

| 담당 | 주기 | 작업 |
|---|---|---|
| 마케팅/사업개발 담당자 | 발견 즉시 (또는 매일 마감 시 일괄) | `공동구매마스터`에 신규 공구 입력, `셀러마스터` 신규 셀러 등록 |
| 사업부 리더 | 주 1회 | `시장수요트래킹` 오픈마켓/홈쇼핑 수동 체크 입력, 대시보드 경고 확인 |
| (2단계) 자동화 배치 | 매일 새벽 | 유튜브·네이버 API로 `시장수요트래킹` 자동 적재 |
| 경영진 | 주 1회 / 필요시 | 대시보드만 열람, 히트예상 TOP10 기반 신규 공구 우선순위 논의 |

---

## 8. 로드맵

- **1단계 (이번 작업, 즉시)**: 설계서 + 구글시트 스키마(xlsx) + 대시보드 MVP (수동 입력 기반)
- **2단계**: 유튜브 API·네이버 오픈API 자동 적재 배치 스크립트 (GitHub Actions cron), API 키 발급 필요
- **3단계**: 6개월+ 데이터 축적 후 통계/ML 기반 예측 모델 고도화, 인스타그램 공식 연동(비즈니스 계정 파트너십) 검토

---

## 9. 다음 실행 단계 (사용자 액션 필요)

1. `공동구매_모니터링_템플릿.xlsx` 를 Google Sheets로 업로드 (파일 → 가져오기)
2. 시트 공유 설정: 파일 → 공유 → "링크가 있는 모든 사용자(뷰어)" 또는 워크스페이스 제한 공유
3. 파일 → 공유 → 웹에 게시(Publish to web) 진행 후 시트ID·gid 확인
4. `dashboard/index.html` 상단 설정값(`SHEET_ID`)에 반영
5. GitHub 저장소 생성 후 push, GitHub Pages 활성화(Settings → Pages → main branch)
